# Generado o de terceros: no se formatea.
node_modules
.next
out
build
coverage
playwright-report
test-results
pnpm-lock.yaml
next-env.d.ts

# Los tipos de la base los genera la CLI de Supabase; se regeneran, no se editan.
src/tipos/database.types.ts

# SQL y configuracion de Supabase tienen su propio estilo.
supabase/

# El prototipo exportado de Stitch se guarda tal cual lo generó: es una
# referencia visual de terceros, no código del proyecto (ver su LEEME.md).
DOC/Maquetas/Stitch/*.html

# Las maquetas de la sesion de diseño del plan 04 se guardan tal cual salieron
# de esa sesion (ver su LEEME.md): son fragmentos de referencia, no codigo.
DOC/Maquetas/4/*.html
